#include<stdio.h>
int main()
{
    double a,b,x;
    x=(a*a+2*a*b+b*b);
    printf("enter the number in a:");
    scanf("%lf",&a);
    printf("enter the number in b:");
    scanf("%lf",&b);
    printf("result is:%lf\n",x);
    return 0;

}
